import { colors } from '../../constants'

export const defaultStyle = {
  container: {
    gap: 8,
  },
  itemContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  itemLabel: {
    fontFamily: 'poppins400',
    fontSize: 18,
    marginTop: 4,
    color: colors.text,
  },
}
